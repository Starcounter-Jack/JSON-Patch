/* this file doesn't need to be transpiled,
it's excluded in tsconfig */

import jsonpatch from './json-patch';
import JsonObserver from './json-observe';

export default jsonpatch;
export { jsonpatch, JsonObserver };